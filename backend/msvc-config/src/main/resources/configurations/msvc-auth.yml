server:
  port: 8081

spring:
  application:
    name: msvc-auth

  datasource:
    url: jdbc:postgresql:http://msvc-config:5432/auth_db
    username: postgres
    password: Redimi2one2025*
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

application:
  security:
    jwt:
      secret-key: 9f8d7a6s5d4f3g2h1j0k9l8m7n6b5v4c3x2z1q
      expiration: 86400000

eureka:
  client:
    service-url:
      defaultZone: http://msvc-config:8761/eureka